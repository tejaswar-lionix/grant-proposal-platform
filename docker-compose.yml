version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: grant
      POSTGRES_USER: grant
      POSTGRES_PASSWORD: grant
    ports: ["5432:5432"]
  web:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
