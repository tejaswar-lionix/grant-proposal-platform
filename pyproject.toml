[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "grant-proposal-platform"
version = "1.0.0"
description = "Grant Proposal Platform"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Proprietary"}
